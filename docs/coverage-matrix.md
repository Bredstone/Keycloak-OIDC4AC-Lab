# OIDC4AC Coverage Matrix

- Keycloak baseline: `6c73e3027811d9c7b22683edd825e839272e9547`
- Specification baseline: `fc7c2c5d5155530cd4b71ee44f37cf11e2a578c2`
- Author decisions: PD-001 through PD-010 in `protocol-decision-log.md`

| Requirement | Status | Implementation | Tests | Limitation / follow-up |
| --- | --- | --- | --- | --- |
| Feature disabled by default | Implemented | `Profile.Feature.OIDC4AC`; guarded integration hooks | Disabled-profile discovery and authorization regression | The disabled profile intentionally uses the built-in browser flow because the experimental planner provider is unavailable. |
| Claim grammar and invalid request | Implemented | `AmrDetailsRequestParser`, authorization checker | `AmrDetailsRequestParserTest`; HTTP malformed-request, null-claim, identifier-values, numeric-bound, signed-JARM, and disabled-realm scenarios; encrypted-JARM error decryption in `OIDC4ACAuthenticationContextTest`; disabled-feature discovery/authorization integration regression | Additional JWE algorithms and key-management combinations remain deployment-specific. |
| Recursive `all_of` / `one_of` and essentiality | Implemented | evaluator and planner | `AmrDetailsRequestParserTest`; HTTP nested, best-effort, and branch scenarios | Browser suite exercises the configured multi-factor planner; arbitrary provider combinations remain deployment-specific. |
| Best-effort retry / SSO freshness | Implemented | `AuthenticationRequirementsPlanner`, `OIDCLoginProtocol.requireReauthentication` | `AmrDetailsRequestParserTest`, `OIDC4ACReauthenticationTest`; HTTP SSO and `max_age` scenarios | Default behavior retries the configured browser flow; an opt-in factor planner can route configured browser subflows. |
| Dynamic browser factor plan | Implemented (opt-in) | `OIDC4ACFactorPlannerAuthenticator`, `AuthenticationFactorPlanPlanner`, plan-aware `DefaultAuthenticationFlow` | Planner, immutable-progress, cancellation-policy unit tests; 9 browser scenarios; Authentication-tab admin API regression; Admin Console OIDC4AC policy-editor test | The policy editor is available when the Admin UI extension surface and OIDC4AC feature are enabled; arbitrary realm flow layouts remain deployment-specific. |
| Immutable event and mandatory `time` | Implemented | grant snapshot store/codec/recorder | `AuthenticationEventSnapshotCodecTest`, `AuthenticationEventSnapshotStoreTest`, `OIDC4ACAuthenticationContextTest`; HTTP repeated-execution, old-grant, and offline-refresh scenarios | Cluster persistence remains deployment-specific. |
| Password adapter | Implemented for the native credential model | Native provider exposes safe password facts when available, including algorithm, iteration count, and credential-update time | Capability and browser lifecycle tests; HTTP credential-metadata and disclosure scenarios | Other password credential providers may expose fewer facts and must omit unavailable properties rather than infer them. |
| OTP adapter | Implemented | Verified selected credential is handed to the native provider, which emits only safe OTP profile data | `NativeAuthenticationMethodDetailsProviderTest`; HTTP and browser OTP lifecycle scenarios | Add credential-policy variants beyond the lab fixture. |
| WebAuthn and passwordless | Implemented mapping | Native provider emits `pop` only | `NativeAuthenticationMethodDetailsProviderTest`; browser virtual-CTAP2 registration and assertions | Hardware authenticator/attestation variants remain deployment-specific. |
| ID Token and UserInfo projection | Implemented | grant-bound delivery service and projection | `AmrDetailsProjectionTest`, `AuthenticationEventSnapshotStoreTest`, `OIDC4ACAuthenticationContextTest`; HTTP null-claim, multi-method, independent-projection, old-grant, SSO, and concurrent-grant scenarios | Each location retains the complete execution set while selecting optional fields. |
| Realm/client disclosure policy | Implemented | Per-field `default`, `requested`, and `never` modes are intersected conservatively before essential evaluation and projection; the Admin Console presents only these selectors | `OIDC4ACDisclosurePolicyTest`; HTTP metadata/property enforcement and requested-only mode scenarios; Admin Console OIDC4AC policy editor | Unset realm fields use the default mode; an empty client override inherits the realm policy. |
| Standard `amr` consistency | Implemented | delivery service merges delivered identifiers | Projection tests and HTTP token assertions | None in the covered lab paths. |
| No access-token `amr_details` | Implemented | only ID Token/UserInfo transformer hooks | HTTP access-token and refresh assertions; `OIDC4ACAuthenticationContextTest` | None in the covered token paths. |
| Discovery | Implemented | capability-derived native metadata augmentation, including custom metadata names | `OIDC4ACDiscoveryMetadataTest`, `CustomAuthenticationMethodDetailsProviderTest`; enabled/disabled well-known integration regression | None in the covered discovery paths. |
| Realm OIDC4AC switch | Implemented | `oidc4ac.enabled` realm attribute gates discovery, planning, validation, snapshots, and delivery; Admin Console Realm settings tab persists it | `OIDC4ACRealmSettingsTest`; HTTP disable/restore discovery and authorization scenario; Admin Console policy-editor test | Existing realms default to enabled for backwards compatibility; assurance-specific policy remains deployment work. |
| Optional authentication metadata | Implemented (safe subset) | Native adapters advertise and emit `iss` and a protocol-conformant structured `location` object containing `ip_address` when the request context provides one; disclosure policy filters both. Evidence-bearing custom providers may add open-valued metadata. | `NativeAuthenticationMethodDetailsProviderTest`; discovery capability test; packaged email SPI Arquillian discovery/ID Token/UserInfo scenario | Native adapters do not infer geospatial/address fields, `trust_framework`, or `assurance_level`; those require an evidence-bearing custom provider. |
| Authentication-method SPI extensibility | Implemented contract | private `AuthenticationMethodDetailsProvider` / factory SPI | `CustomAuthenticationMethodDetailsProviderTest` and the packaged `oidc4ac-test-email` provider fixture exercise authenticator, token projection, and discovery contribution through the real Arquillian server | A separately deployed production provider remains deployment work; the test fixture is packaged through the Arquillian provider module. |
| Essential failure response | Implemented for authorization endpoint | OIDC final evaluator and browser error bridge | `OIDC4ACAuthenticationFailureBridgeTest`; HTTP generic essential-failure, invalid-client/redirect, state-preserving query/fragment/form_post, and signed JARM scenarios; encrypted RSA-OAEP/A256GCM JARM decryption in `OIDC4ACAuthenticationContextTest`; browser missing-passkey and explicit-cancellation scenarios | Additional JWE algorithms and key-management combinations remain deployment-specific. |
| Refresh and later UserInfo | Implemented (unit and lab coverage) | opaque token handle selects the server-side grant snapshot | `AuthenticationEventSnapshotStoreTest`; `OIDC4ACAuthenticationContextTest`; HTTP offline refresh, old-grant, concurrent-grant, and later-UserInfo scenarios; `OIDC4ACGrantSnapshotFailoverClusterTest` source-compiles | Multi-node runtime validation remains deployment-specific; the local Arquillian cluster profile returned 503 before starting its backend adapters. |
| Integration laboratory | Implemented | Standalone Keycloak/test-client fixtures in this repository | 36 HTTP scenarios (including JARM, realm-switch, metadata, disclosure-mode, malformed-grammar, expression-free essential claims, credential metadata, and constraint coverage), 9 browser/WebAuthn scenarios, disabled-feature integration regression, 5 focused Keycloak authorization tests, and a preserved custom-provider integration fixture | The lab requires a Keycloak checkout or compatible image; generated `.runtime/` and `.build/` state is local-only. |

The HTTP metadata scenarios additionally verify the advertised `iss` and
structured `location.ip_address` fields and their disclosure-policy behavior.
